---
title: "Connect"
language: en
icon: "plug"
description: "Deploy your agent through channels, external integrations, and API so users can talk to it wherever they are."
---

<Info>
  Before connecting your agent, make sure your **Identity**, **Knowledge**, and **Skills** are ready.  
  A well-configured agent gives users better answers from the first conversation.
</Info>

## Connect your agent to real users

The **Connect** page is organized into two tabs:

- **Channels** — connect and manage the channels where your agent talks to users.
- **Messaging Behaviour** — configure how your agent handles messages on each connected channel.

This is the step that turns your agent from "configured" into "live".

---

## Channels tab

The Channels tab is organized in 3 sections:

- **Channels**: publish your agent in owned and external channels (Web Bubble, External Chat, WhatsApp, and others)
- **Integrations**: connect external tools/services used by your operation
- **API**: send and receive messages from your own systems

### External Chat (fastest launch)

**External Chat** gives you a shareable URL you can send in seconds.

<Steps>
  <Step title="Go to Connect > Channels">
    Open your agent and navigate to the Connect page.
  </Step>
  <Step title="Open External Chat">
    Select the External Chat channel card.
  </Step>
  <Step title="Copy the public URL">
    Click the share option and copy the generated link.
  </Step>
  <Step title="Test as an end user">
    Open the link in an incognito window and run a few real use-case questions.
  </Step>
</Steps>

### Web Bubble (embed in your website)

Use **Web Bubble** when you want the agent inside your own site.

<Steps>
  <Step title="Open Bubble channel">
    In Connect > Channels, open the Bubble card.
  </Step>
  <Step title="Generate credentials/snippets">
    The Console provides the snippet and keys needed for embedding.
  </Step>
  <Step title="Add snippet to your website">
    Paste the script where your implementation team manages site code.
  </Step>
  <Step title="Customize appearance">
    Configure brand settings like launcher style and welcome behavior.
  </Step>
</Steps>

### Meta channels (WhatsApp, Messenger, Instagram)

The Connect page always displays a **Meta** section with three channel cards:

| Channel | Description |
|---|---|
| **WhatsApp** | WhatsApp Business channel via Meta |
| **Messenger** | Facebook Messenger channel via Meta |
| **Instagram** | Instagram messaging channel via Meta |

Each card shows a **Connect** button that opens the setup flow for that specific channel. You do not need to configure the Meta integration first — the cards are always visible so you can start the connection process at any time.

Once you have connected one or more accounts for a channel type, a **Channels (n)** button appears on the card showing the count of active connections. Click it to view, edit, or reconnect your existing connections.

---

## Messaging Behaviour tab

The **Messaging Behaviour** tab lets you fine-tune how the agent processes and responds to messages on each connected channel. Settings are configured **per channel**.

### Selecting a channel

At the top of the tab you'll see a row of **channel selector pills** — one for each connected channel. Each pill shows the channel name, type, and identifier. Click a pill to select that channel; it highlights to indicate it's active.

If no channels are connected yet, the tab displays a message prompting you to connect a channel first.

### Debounce time

The **Debounce Time** setting controls how long the agent waits after receiving a message before processing and responding. This is useful for channels where users tend to send multiple short messages in rapid succession (like WhatsApp).

- Adjust the slider between **0 s** and **15 s**, or use the quick-select buttons (**0, 1, 3, 5, 8, 10, 15**).
- Setting debounce to **0 s** means the agent responds immediately to every message.
- A visual **preview** below the slider shows how multiple user messages are batched during the debounce window before the agent responds.

<Note>
The debounce setting is automatically hidden for internal channels such as the **Playground** and **Agent API**, since batching does not apply to those channel types.
</Note>

<Tip>
Start with a debounce of **3–5 seconds** for WhatsApp and similar channels. This gives users enough time to finish typing without making the agent feel slow.
</Tip>

### File preferences

Below the debounce section, you can toggle which **file types** the agent is allowed to receive on the selected channel:

| File type | Examples |
|---|---|
| **Images** | Photos, screenshots, scanned documents |
| **Documents** | PDFs, Word files, spreadsheets |
| **Audio** | Voice notes, audio recordings |
| **Video** | Video clips, screen recordings |

For each enabled file type, configure:
- **Max size** — the maximum allowed file size (in MB)
- **Instructions** — guidance the agent follows when it receives this type of file (e.g. "Describe the image content and answer the user's question based on it")

Disabled file types are ignored by the agent on that channel.

### Saving changes

After adjusting debounce or file preferences for a channel, click **Save** in the top-right area. A brief confirmation appears once the settings are persisted.

<Note>
Session close (automatic conversation closing after inactivity) is now configured in **Proactive AI > Engagement > Follow ups**. See the [Follow ups](/contacts/en/followups) documentation for details.
</Note>
